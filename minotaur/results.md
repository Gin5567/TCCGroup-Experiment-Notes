# Ariane / MINOTAuR 批量测试记录

## 最新统计快照：2026-09-21 02:26

数据来源：服务器 `~/research_ariane/server_batch/results.csv`。

本页记录统计截止时刻的结果。后续重跑可能产生新记录，因此下表不代表全部测试已经结束。

### 运行尝试与测例数量

| 项目                                                  | 数量 |
| ----------------------------------------------------- | ---- |
| 不同测例                                              | 51   |
| 运行尝试总数，包含初跑与重跑                          | 139  |
| OK运行记录                                            | 93   |
| TIMEOUT运行记录                                       | 41   |
| INTERRUPTED运行记录                                   | 5    |
| 同输入、两分支均成功的配对测例                        | 46   |
| 配对测例中 Ariane 的 LSU inversion 非零、MINOTAuR 为0 | 27   |

### 各分支的可用状态

| 分支     | OK   | TIMEOUT | INTERRUPTED | 说明                                                    |
| -------- | ---- | ------- | ----------- | ------------------------------------------------------- |
| Ariane   | 47   | 4       | 0           | 当前汇总覆盖51个测例                                    |
| MINOTAuR | 46   | 3       | 1           | 当前状态汇总只覆盖50个测例，尚有1个测例的记录情况待核对 |

### 统计口径

1. 保留初跑、超时、中断和重跑记录，不覆盖历史尝试。
2. 按 `core + app + mem_sha256` 区分结果，并核对代码版本与实验配置是否一致。
3. 对同一组内的多次尝试，优先选取成功记录；多次成功结果应检查关键指标是否一致，并记录所选记录。
4. 同一测例只有在两分支均为OK、输入哈希一致且配置可比时，才纳入配对分析。
5. TIMEOUT、INTERRUPTED或缺失结果中的空字段表示“未取得”，不能当作0。
6. `elapsed_seconds`表示主机运行耗时；`total_time`和`base_time`表示程序报告的仿真周期。

### 周期与指令数的计算

沿用 bsort 单例的统计口径：

- 主体周期：`total_time - base_time`
- 主体提交指令数：`total_committed - base_committed`
- 主体CPI：`主体周期 / 主体提交指令数`
- 主体周期开销：`(MINOTAuR主体周期 / Ariane主体周期 - 1) × 100%`

### 当前结论

在46个同输入、双分支均成功的配对测例中，27个测例在 Ariane 上记录到非零 LSU inversion，而在 MINOTAuR 上降为0。这表明，此前在 bsort 中观察到的计数变化也出现在其他基准程序中。

该结果支持“MINOTAuR在这些运行中消除了已记录的LSU inversion”这一观测结论，尚不能单独证明 Ariane 出现了严格意义上的 timing anomaly，也不能证明所有程序和输入都不存在时序异常。

整体周期开销仍需依据完整配对数据计算，不能将 bsort 的约0.955%开销推广到全部测例。

### 待补齐内容

- 导出46个配对测例的逐项数据，列出两分支LSU计数、主体周期、主体提交指令数及周期开销。
- 列出未形成有效配对的测例、各分支状态和后续处理。
- 核对MINOTAuR状态汇总中尚未覆盖的1个测例。
- 检查重跑记录的代码版本、配置和输入是否一致。
- 核对实际可用的51个测例与论文所述测例集合之间的差异。

## 历史快照（2026-09-18 查询快照）

服务器结果文件：`~/research_ariane/server_batch/results.csv`。查询时该文件有 115 行（含表头），即 114 条**运行记录**；其中包括初跑及后续重跑，不能当作 114 个不同测例。重跑仍在进行，本页只列出已经从 CSV 原文核实的记录。

| 测例 | 分支 | 尝试 | 状态 | 主机耗时（秒） | `lsu` | `total_time`（周期） |
| --- | --- | --- | --- | ---: | ---: | ---: |
| `bsort` | Ariane | 初跑 | OK | 2,280.3 | 2 | 60,862 |
| `bsort` | MINOTAuR | 初跑 | OK | 3,147.5 | 0 | 61,610 |
| `filterbank` | Ariane | 初跑 | TIMEOUT | 7,200.3 | — | — |
| `filterbank` | MINOTAuR | 初跑 | TIMEOUT | 7,200.0 | — | — |
| `fir2dim` | Ariane | 初跑 | OK | 438.6 | 0 | 35,975 |
| `fir2dim` | MINOTAuR | 初跑 | OK | 540.7 | 0 | 38,242 |
| `fmref` | Ariane | 初跑 | TIMEOUT | 7,200.3 | — | — |
| `fmref` | MINOTAuR | 初跑 | TIMEOUT | 7,200.1 | — | — |
| `fmref` | Ariane | 重跑 | OK | 16,240.9 | 1 | 7,217,985 |

`rerun_parallel_8.progress.log` 在这次查询时显示“待重跑 30 项；排除：mpeg2, susan”，并记录了部分已完成的重跑。上表中的 Ariane `fmref` 已从初跑超时变为重跑成功；**MINOTAuR 的 `fmref` 在本快照中仍只有初跑超时记录**，不能据此判定其最终结果。TIMEOUT 行为空的 `lsu` 和周期是“未取得”，不是零。

本页是局部快照。若要统计全体测例的最终成功率或比较周期，需在重跑结束后按 `core + app + mem_sha256` 汇总各次尝试，并分别保留初跑结果与最终可用结果。`bsort` 的逐项解释见 [bsort.md](bsort.md)。
